# company-builder-experiment — run-1 CLOSED

One autonomous run that took a company from pain-point hunt to a packaged launch: **BatchCOGS**, recipe-based COGS tracking for handmade Etsy/Shopify sellers ($19/mo). Finished 2026-07-24, independent final evaluation PASS 10/10.

Contract and gates: `run-1/PROMPT-CONTRACT.md`, `run-1/GATES.md`. Start reading at `run-1/recap.html`; decisions and the two gate failures are in `run-1/BUILD-LOG.md`. **Gotcha:** the Workflow scripts hit token and WebSearch rate limits at high parallelism — budget per agent and null-harden every stage (detail in this project's memory, `wf-rate-limit-lessons`).

**Open item** (human, ~5 min): whether Stocksmith Studio's $41-49 tier includes auto-deduction — needs a free trial account. It gates the wedge claim in the business docs.
