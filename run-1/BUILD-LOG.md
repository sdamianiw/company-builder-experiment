# Build Log — run-1

Running log of decisions, self-answered questions, gate results, and cuts. Newest at the bottom. Format: **[phase] Decision/Q** — answer — why.

## 2026-07-11

- **[P0] Q: The master prompt names API keys in `.env` — do they exist here?** No. Verified: no `.env` in this project; global filename+content sweep found the key names only in this session's own transcript. **Why it matters:** videos/images must use free local tooling (guardrail 1 allows only already-keyed APIs). Cut logged: premium avatar/voice/image quality.
- **[P0] Q: "Founder script passes my voice rules" — whose rules?** The video author's; no voice-rules file exists in this environment. **Decision:** write explicit voice rules in `brand/voice-rules.md` (labeled inference) and gate the script on them. Better a stated, checkable standard than an invisible one.
- **[P0] Decision: `git init` (local).** The dir wasn't a repo; checkpointing protects a long autonomous run. Publishes nothing → guardrail 2 intact.
- **[P0] Decision: model budget.** Sonnet for fan-out research/mechanical builds, Opus for judgment (judging, thesis, red team). Main loop orchestrates + verifies only. Reason: context survival + effort matched to task complexity.
- **[P0] GATE G0 (RED): PASS.** Pipeline proven end-to-end: HTML → Edge headless (`--headless=new` + isolated profile; plain `--headless` fails silently — noted) → PNG (visually verified) → ffmpeg-static + System.Speech wav → smoke.mp4 (3.65s, h264+aac) → frame re-extracted and viewed. Voices available: Zira (en-US, female) only for English → the founder voice is Zira; Sabina/Helena are Spanish. WebFetch verified working.
- **[P0] Decision: renderer = Microsoft Edge headless** (Chrome not installed; Edge supports identical flags). Screenshot tool for site verification will use the same path.
- **[P0-revision] Decision: renderer = Playwright bundled Chromium, NOT Edge.** Edge auto-updated mid-session (149→150; BITS updater artifacts in %TEMP%) and began exiting silently without writing screenshots — non-deterministic system state. Pivoted per premortem F3 fallback chain. Playwright Chromium is isolated from system browsers and also captures console errors (feeds gate G4). Verified: mobile (390x844) + desktop (1440x900) shots OK; linkcheck.mjs proven to detect broken links (2/4 test case).
- **[P0.2] Video pipeline proven end-to-end** (`tools/render-video.mjs` window.seek(t) frame stepping → `tools/tts.ps1` Zira wav → `tools/encode-video.mjs` h264+aac mp4 → frame re-extracted and visually verified: t=0.50s frame showed exactly t=0.50s). Fixed en route: tts.ps1 relative-path bug; encode-video.mjs stray probe call. P5 risk retired.
- **[P1] GATE G1 (RED): FAIL — round 1.** 28 pains found, top 8 clustered, **0/8 survived adversarial verification** (2 REFUTED, 6 WEAK). Root causes per skeptics+critic: (a) researchers cited vendor comparison blogs and 2 AI-generated fake sources (beancount.io "forum", replit.app site) instead of primary community evidence; (b) "no alternative exists" framings refuted by free incumbents (Zoho Invoice, AbleSpace, Innago, HomeBeacon, Trackery); (c) headline stats misattributed to URLs that don't contain them. Skeptic layer worked exactly as designed — fiction stopped before entering the thesis.
- **[P1] Methodology fix for round 2 (self-improvement loop):** (1) primary community sources ONLY (real Reddit/HN/forum threads; vendor domains banned as primary evidence); (2) mandatory free-incumbent check by the researcher before proposing, stating the surviving persona-specific wedge; (3) recalibrated verify rubric — mission says *underserved*, not *unserved*: evidenced wedge in crowded category = viable; fabricated/misattributed stats or wedge-already-filled = kill; (4) rescue-verify strongest round-1 leads (Etsy maker COGS, USCIS form prep, no-show deposits) with proper re-scoping. Evidence bar RAISED while competition bar recalibrated to mission language.

## 2026-07-23 (session resumed after terminal close + rate-limit death)

- **[P1] Round-2 workflow `wf_a9d971b6-cc6` post-mortem.** 8/9 agents killed by session rate limit ("resets 6:10pm Europe/Berlin", 2026-07-11); only `rescue-etsy-cogs` completed. Run burned 641k tokens for ~11% completion — hunters averaged ~100k tokens / ~130 tool calls each (token-obese, the direct cause of hitting the limit). Script then crashed on unguarded `ranked.top` (rank-judge returned null).
- **[P1] Surviving output: `evidence/raw/rescue-etsy-cogs.md` — WEDGE SURVIVES (NARROWED).** Etsy/Shopify handmade sellers priced out of Craftybase→Stocksmith's 5x hike ($19→$99/mo); $19/mo alternatives (Inventora, Ardent Seller) have documented reliability bugs or zero market awareness. 6+ named reviewers across Capterra/SoftwareAdvice/Shopify App Store, all quotes traced to fetched URLs. Severity 3.5/5. One verified-grade candidate in hand.
- **[P1] New standing directive (user): workflows must be token-lean.** Applied to all remaining workflows: explicit per-agent tool-call budgets, effort overrides matched to task, skip work already on disk, null-hardened stages with graceful partial returns.
- **[P1] Decision: round 2b = fresh hardened run, NOT resumeFromRunId.** Resume caching is prefix-based on unchanged (prompt,opts); the token-lean prompt edits bust the cache anyway, the completed agent's value is injected via `args.rescued`, and the old script carries the `ranked.top` crash bug. G1 decision rule pre-committed: >=4 VERIFIED -> pass; 2-3 -> narrower tournament, log the cut; <2 -> ONE targeted mini-sweep then proceed with best available. Never a full round 3.
